<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Quiz Results | StudentLink</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #f8fafc; }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6">
    <div class="max-w-md w-full bg-white p-10 rounded-[3rem] shadow-2xl text-center border border-slate-100">
        <div class="w-24 h-24 bg-teal-50 text-teal-600 rounded-full flex items-center justify-center mx-auto mb-6 border-4 border-white shadow-lg">
            <i class="fa-solid fa-graduation-cap text-4xl"></i>
        </div>
        
        <h1 class="text-2xl font-black text-slate-900 mb-1">Attempt Completed!</h1>
        <p class="text-slate-400 text-sm font-bold uppercase tracking-widest mb-8"><%= quizTitle %></p>

        <div class="bg-slate-50 rounded-[2rem] p-8 mb-8">
            <div class="text-[4rem] font-black text-teal-800 leading-none"><%= score %></div>
            <div class="text-xs font-black text-slate-400 uppercase tracking-tighter mt-2">Points Earned</div>
            
            <div class="mt-6 pt-6 border-t border-slate-200 grid grid-cols-2 gap-4">
                <div>
                    <p class="text-[10px] font-black text-slate-400 uppercase">Correct</p>
                    <p class="text-lg font-bold text-slate-700"><%= score %> / <%= total %></p>
                </div>
                <div>
                    <p class="text-[10px] font-black text-slate-400 uppercase">Grade</p>
                    <p class="text-lg font-bold text-teal-600"><%= Math.round((score/total) * 100) %>%</p>
                </div>
            </div>
        </div>

        <div class="space-y-3">
            <a href="/dashboard" class="block w-full bg-slate-900 text-white py-4 rounded-2xl font-black uppercase tracking-widest shadow-lg active:scale-95 transition-all">
                Back to Dashboard
            </a>
            <button onclick="window.print()" class="text-xs font-bold text-slate-400 hover:text-teal-600 transition-colors">
                <i class="fa-solid fa-print mr-1"></i> Save Result as PDF
            </button>
        </div>
    </div>
</body>
</html>