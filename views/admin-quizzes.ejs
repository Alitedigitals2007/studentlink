<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Management | Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #f8fafc; }
        .custom-scrollbar::-webkit-scrollbar { width: 6px; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #e2e8f0; border-radius: 10px; }
    </style>
</head>
<body class="text-slate-900">

    <div class="flex min-h-screen">
        <main class="flex-1 p-4 md:p-8">
            
            <header class="flex flex-col md:flex-row justify-between items-start md:items-center mb-10 gap-4">
                <div>
                    <h1 class="text-3xl font-black text-slate-900 tracking-tight">Quiz Control</h1>
                    <p class="text-slate-500 font-medium">Manage CBT sessions and track student scores.</p>
                </div>
                <div class="flex gap-3">
                    <a href="/timeline" class="bg-white p-3 px-6 rounded-2xl shadow-sm border border-slate-100 hover:text-teal-600 transition-all font-bold text-sm flex items-center gap-2">
                        <i class="fa-solid fa-house"></i> Home
                    </a>
                </div>
            </header>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                
                <section class="lg:col-span-1">
                    <div class="bg-white p-8 rounded-[2.5rem] shadow-sm border border-slate-100 sticky top-8">
                        <h3 class="text-lg font-black mb-6 flex items-center gap-2">
                            <i class="fa-solid fa-plus-circle text-teal-600"></i> New Session
                        </h3>
                        <form action="/admin/create-session" method="POST" class="space-y-4">
                            <div>
                                <label class="text-[10px] font-black uppercase text-slate-400 ml-2">Quiz Title</label>
                                <input type="text" name="title" required placeholder="e.g. GST 101 Midterm" class="w-full p-4 bg-slate-50 rounded-2xl border-none outline-none focus:ring-2 ring-teal-500/20 mt-1 text-sm font-bold">
                            </div>
                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                                <div>
                                    <label class="text-[10px] font-black uppercase text-slate-400 ml-2">Start Time</label>
                                    <input type="datetime-local" name="start_time" required class="w-full p-4 bg-slate-50 rounded-2xl border-none outline-none text-xs mt-1 font-bold">
                                </div>
                                <div>
                                    <label class="text-[10px] font-black uppercase text-slate-400 ml-2">End Time</label>
                                    <input type="datetime-local" name="end_time" required class="w-full p-4 bg-slate-50 rounded-2xl border-none outline-none text-xs mt-1 font-bold">
                                </div>
                            </div>
                            <div>
                                <label class="text-[10px] font-black uppercase text-slate-400 ml-2">Duration (Minutes)</label>
                                <input type="number" name="duration" required placeholder="15" class="w-full p-4 bg-slate-50 rounded-2xl border-none outline-none mt-1 text-sm font-bold">
                            </div>
                            <button type="submit" class="w-full bg-teal-800 text-white py-4 rounded-2xl font-black uppercase tracking-widest shadow-lg shadow-teal-900/20 active:scale-95 transition-all mt-2">
                                Initialize Quiz
                            </button>
                        </form>
                    </div>
                </section>

                <section class="lg:col-span-2 space-y-8">
                    
                    <div class="bg-white p-8 rounded-[2.5rem] shadow-sm border border-slate-100">
                        <h3 class="text-lg font-black mb-6">Quiz Sessions</h3>
                        <div class="overflow-x-auto">
                            <table class="w-full text-left">
                                <thead class="text-[10px] font-black uppercase text-slate-400 border-b border-slate-50">
                                    <tr>
                                        <th class="pb-4">Title</th>
                                        <th class="pb-4">Duration</th>
                                        <th class="pb-4">Status</th>
                                        <th class="pb-4 text-right">Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="text-sm">
                                    <% if (sessions.length === 0) { %>
                                        <tr><td colspan="4" class="py-8 text-center text-slate-400 font-bold">No sessions created yet.</td></tr>
                                    <% } %>
                                    <% sessions.forEach(s => { %>
                                        <tr class="border-b border-slate-50 hover:bg-slate-50/50 transition-colors">
                                            <td class="py-4">
                                                <p class="font-bold text-slate-700 leading-none"><%= s.title %></p>
                                                <span class="text-[9px] text-slate-400 uppercase font-black tracking-tighter">ID: #<%= s.id %></span>
                                            </td>
                                            <td class="py-4 font-bold text-slate-500"><%= s.duration_minutes %>m</td>
                                            <td class="py-4">
                                                <% 
                                                    const now = new Date();
                                                    const start = new Date(s.start_time);
                                                    const end = new Date(s.end_time);
                                                %>
                                                <% if (now < start) { %>
                                                    <span class="px-3 py-1 bg-amber-50 text-amber-600 rounded-full text-[9px] font-black uppercase">Upcoming</span>
                                                <% } else if (now > end) { %>
                                                    <span class="px-3 py-1 bg-slate-100 text-slate-500 rounded-full text-[9px] font-black uppercase">Closed</span>
                                                <% } else { %>
                                                    <span class="px-3 py-1 bg-emerald-50 text-emerald-600 rounded-full text-[9px] font-black uppercase animate-pulse">Live</span>
                                                <% } %>
                                            </td>
                                            <td class="py-4">
                                                <div class="flex justify-end gap-2">
                                                    <a href="/admin/quiz/<%= s.id %>/questions" class="w-8 h-8 rounded-lg bg-teal-50 text-teal-600 flex items-center justify-center hover:bg-teal-600 hover:text-white transition-all shadow-sm" title="Manage Questions">
                                                        <i class="fa-solid fa-list-check text-xs"></i>
                                                    </a>
                                                    <form action="/admin/delete-session/<%= s.id %>" method="POST" onsubmit="return confirm('Delete this session and all results?')">
                                                        <button type="submit" class="w-8 h-8 rounded-lg bg-rose-50 text-rose-500 flex items-center justify-center hover:bg-rose-500 hover:text-white transition-all shadow-sm">
                                                            <i class="fa-solid fa-trash-can text-xs"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="bg-white p-8 rounded-[2.5rem] shadow-sm border border-slate-100">
                        <div class="flex justify-between items-center mb-6">
                            <h3 class="text-lg font-black">Student Attempt Logs</h3>
                            <span class="text-[10px] font-black bg-slate-100 px-3 py-1 rounded-full uppercase tracking-widest text-slate-500">Live Updates</span>
                        </div>
                        <div class="space-y-4 max-h-[500px] overflow-y-auto custom-scrollbar pr-2">
                            <% if (attempts.length === 0) { %>
                                <div class="text-center py-10">
                                    <i class="fa-solid fa-user-clock text-slate-200 text-4xl mb-2"></i>
                                    <p class="text-slate-400 font-bold text-sm">Waiting for first submissions...</p>
                                </div>
                            <% } %>
                            <% attempts.forEach(log => { %>
                                <div class="flex items-center justify-between p-4 bg-slate-50 rounded-2xl border border-slate-100 hover:border-teal-200 transition-all group">
                                    <div class="flex items-center gap-4">
                                        <div class="w-12 h-12 bg-white rounded-xl shadow-sm border border-slate-100 flex flex-col items-center justify-center">
                                            <span class="text-xs font-black text-teal-600 leading-none"><%= log.score %></span>
                                            <span class="text-[7px] font-bold text-slate-400 uppercase mt-1">Pts</span>
                                        </div>
                                        <div>
                                            <p class="text-sm font-black text-slate-800 group-hover:text-teal-700 transition-colors"><%= log.fullname %></p>
                                            <p class="text-[9px] text-slate-400 uppercase font-black tracking-wider">
                                                <%= log.quiz_title %> <span class="mx-1">•</span> 
                                                <i class="fa-solid fa-stopwatch mr-0.5"></i> <%= log.time_spent %>
                                            </p>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <span class="text-[10px] font-black text-slate-400 block">
                                            <%= new Date(log.finish_time).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %>
                                        </span>
                                        <span class="text-[8px] font-bold text-slate-300 uppercase">Completed</span>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                </section>
            </div>
        </main>
    </div>
</body>
</html>