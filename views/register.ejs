<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"><title>Quiz Results | StudentLink</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-[#f8fafc] min-h-screen flex items-center justify-center p-6">
    <div class="max-w-md w-full bg-white p-10 rounded-[3.5rem] shadow-2xl text-center border border-slate-100 relative overflow-hidden">
        <div class="absolute -top-24 -right-24 w-48 h-48 bg-teal-50 rounded-full blur-3xl"></div>
        
        <div class="w-24 h-24 <%= score/total >= 0.5 ? 'bg-teal-50 text-teal-600' : 'bg-rose-50 text-rose-600' %> rounded-full flex items-center justify-center mx-auto mb-6 border-4 border-white shadow-xl relative z-10">
            <i class="fa-solid <%= score/total >= 0.5 ? 'fa-award' : 'fa-circle-exclamation' %> text-4xl"></i>
        </div>
        
        <h1 class="text-2xl font-black text-slate-900 mb-1">
            <%= score/total >= 0.5 ? 'Great Job!' : 'Keep Practicing!' %>
        </h1>
        <p class="text-slate-400 text-[10px] font-black uppercase tracking-[0.2em] mb-8"><%= quizTitle %></p>

        <div class="bg-slate-50 rounded-[2.5rem] p-10 mb-8 border border-slate-100">
            <div class="text-[5rem] font-black text-slate-900 leading-none tracking-tighter">
                <%= score %><span class="text-2xl text-slate-300">/</span><span class="text-2xl text-slate-300"><%= total %></span>
            </div>
            <div class="text-[10px] font-black text-slate-400 uppercase tracking-widest mt-4">Total Score</div>
            
            <div class="mt-8 grid grid-cols-2 gap-4">
                <div class="bg-white p-4 rounded-3xl shadow-sm">
                    <p class="text-[10px] font-black text-slate-400 uppercase mb-1">Percentage</p>
                    <p class="text-xl font-black text-teal-600"><%= Math.round((score/total) * 100) %>%</p>
                </div>
                <div class="bg-white p-4 rounded-3xl shadow-sm">
                    <p class="text-[10px] font-black text-slate-400 uppercase mb-1">Status</p>
                    <p class="text-xl font-black <%= score/total >= 0.5 ? 'text-emerald-500' : 'text-rose-500' %>">
                        <%= score/total >= 0.5 ? 'PASSED' : 'FAILED' %>
                    </p>
                </div>
            </div>
        </div>

        <div class="space-y-4">
            <a href="/dashboard" class="group block w-full bg-slate-900 text-white py-5 rounded-2xl font-black uppercase tracking-widest shadow-xl hover:shadow-2xl active:scale-95 transition-all">
                Return to Dashboard
                <i class="fa-solid fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
            </a>
            <button onclick="window.print()" class="flex items-center justify-center w-full gap-2 text-xs font-black text-slate-400 hover:text-teal-600 transition-colors">
                <i class="fa-solid fa-file-pdf"></i> GENERATE RESULT SLIP
            </button>
        </div>
    </div>
</body>
</html>