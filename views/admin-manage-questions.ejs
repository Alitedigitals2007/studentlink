<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Questions | <%= session.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #f8fafc; color: #0f172a; }
        .code-font { font-family: 'Courier New', Courier, monospace; }
    </style>
</head>
<body class="pb-10">

    <header class="p-6 bg-white border-b border-slate-100 sticky top-0 z-50 flex items-center justify-between">
        <div class="flex items-center gap-4">
            <a href="/admin/quizzes" class="w-10 h-10 rounded-xl bg-slate-50 flex items-center justify-center text-slate-400 hover:text-teal-600 transition-all">
                <i class="fa-solid fa-arrow-left"></i>
            </a>
            <div>
                <h1 class="text-xl font-black"><%= session.title %></h1>
                <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Question Management</p>
            </div>
        </div>
        <div class="bg-teal-800 text-white px-5 py-2 rounded-xl text-xs font-black">
            <%= questions.length %> Questions
        </div>
    </header>

    <main class="max-w-7xl mx-auto p-6 grid grid-cols-1 lg:grid-cols-12 gap-8">
        
        <aside class="lg:col-span-5 space-y-6">
            
            <div class="bg-slate-900 text-slate-300 p-6 rounded-[2rem] shadow-xl">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-[10px] font-black uppercase tracking-widest text-teal-400">JSON Template</h3>
                    <button onclick="copyTemplate()" class="text-[10px] bg-slate-800 px-3 py-1 rounded-lg hover:bg-teal-600 hover:text-white transition-all">
                        <i class="fa-solid fa-copy mr-1"></i> Copy
                    </button>
                </div>
                <pre id="jsonTemplate" class="text-[10px] leading-relaxed code-font overflow-x-auto">[
  {
    "question": "What is 2 + 2?",
    "option_a": "3",
    "option_b": "4",
    "option_c": "5",
    "option_d": "6",
    "correct_option": "B"
  },
  {
    "question": "Capital of France?",
    "option_a": "Berlin",
    "option_b": "Madrid",
    "option_c": "Paris",
    "option_d": "Rome",
    "correct_option": "C"
  }
]</pre>
            </div>

            <div class="bg-white p-6 rounded-[2rem] shadow-sm border border-slate-100">
                <h3 class="text-xs font-black uppercase tracking-widest text-slate-400 mb-4">Bulk Paste Import</h3>
                <form action="/admin/quiz/<%= session.id %>/import-text" method="POST" class="space-y-4">
                    <textarea name="jsonText" id="importBox" required rows="10" 
                        class="w-full p-4 bg-slate-50 rounded-2xl text-[11px] code-font border-none outline-none focus:ring-2 ring-teal-500/20"
                        placeholder="Paste your JSON array here..."></textarea>
                    
                    <button type="submit" class="w-full bg-teal-800 text-white py-4 rounded-xl font-black text-[10px] uppercase tracking-widest shadow-lg shadow-teal-900/20 active:scale-95 transition-all">
                        Process and Save Questions
                    </button>
                </form>
            </div>
        </aside>

        <section class="lg:col-span-7 space-y-4">
            <h3 class="text-xs font-black uppercase tracking-widest text-slate-400 ml-4">Current Question Bank</h3>
            
            <% if(questions.length === 0) { %>
                <div class="bg-white rounded-[2rem] p-20 text-center border border-dashed border-slate-200">
                    <p class="text-slate-400 font-bold">No questions yet. Use the template on the left to add some!</p>
                </div>
            <% } %>

            <% questions.forEach((q, i) => { %>
                <div class="bg-white p-6 rounded-[2rem] shadow-sm border border-slate-100">
                    <div class="flex justify-between items-center mb-3">
                        <span class="text-[10px] font-black text-slate-400">#<%= i + 1 %></span>
                        <form action="/admin/delete-question/<%= q.id %>" method="POST">
                            <button class="text-slate-300 hover:text-rose-500 transition-all"><i class="fa-solid fa-trash-can"></i></button>
                        </form>
                    </div>
                    <p class="font-bold text-slate-800 mb-4"><%= q.question %></p>
                    <div class="grid grid-cols-2 gap-2">
                        <div class="p-2 rounded-lg text-[10px] <%= q.correct_option === 'A' ? 'bg-teal-50 text-teal-700 font-black border border-teal-100' : 'bg-slate-50 text-slate-500' %>">A: <%= q.option_a %></div>
                        <div class="p-2 rounded-lg text-[10px] <%= q.correct_option === 'B' ? 'bg-teal-50 text-teal-700 font-black border border-teal-100' : 'bg-slate-50 text-slate-500' %>">B: <%= q.option_b %></div>
                        <div class="p-2 rounded-lg text-[10px] <%= q.correct_option === 'C' ? 'bg-teal-50 text-teal-700 font-black border border-teal-100' : 'bg-slate-50 text-slate-500' %>">C: <%= q.option_c %></div>
                        <div class="p-2 rounded-lg text-[10px] <%= q.correct_option === 'D' ? 'bg-teal-50 text-teal-700 font-black border border-teal-100' : 'bg-slate-50 text-slate-500' %>">D: <%= q.option_d %></div>
                    </div>
                </div>
            <% }) %>
        </section>
    </main>

    <script>
        function copyTemplate() {
            const text = document.getElementById('jsonTemplate').innerText;
            navigator.clipboard.writeText(text);
            alert("Template copied! Now paste it into the box below and edit.");
        }
    </script>
</body>
</html>