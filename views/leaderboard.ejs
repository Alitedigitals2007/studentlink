<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard | Hall of Fame</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #f8fafc; color: #0f172a; }
        .podium-gold { background: linear-gradient(135deg, #fbbf24 0%, #d97706 100%); }
        .podium-silver { background: linear-gradient(135deg, #cbd5e1 0%, #64748b 100%); }
        .podium-bronze { background: linear-gradient(135deg, #fb923c 0%, #c2410c 100%); }
        .rank-card { transition: transform 0.2s ease; }
        .rank-card:hover { transform: scale(1.02); }
    </style>
</head>
<body class="pb-24">

    <header class="p-6 flex items-center justify-between sticky top-0 bg-white/80 backdrop-blur-md z-50 border-b border-slate-100">
        <a href="/timeline" class="w-10 h-10 bg-white rounded-xl flex items-center justify-center shadow-sm text-slate-400 border border-slate-100">
            <i class="fa-solid fa-chevron-left"></i>
        </a>
        <h1 class="text-lg font-black tracking-tight uppercase">Hall of Fame</h1>
        <div class="w-10"></div> </header>

    <main class="max-w-md mx-auto px-6 pt-8">
        
        <% if (winners.length >= 3) { %>
            <div class="flex items-end justify-center gap-2 mb-12 mt-10 h-48">
                <div class="flex flex-col items-center">
                    <img src="<%= winners[1].profile_pic || 'https://ui-avatars.com/api/?name=' + winners[1].fullname %>" class="w-12 h-12 rounded-full border-4 border-white shadow-lg mb-2">
                    <div class="podium-silver w-20 h-24 rounded-t-2xl flex flex-col items-center justify-center text-white">
                        <span class="text-2xl font-black">2</span>
                        <span class="text-[8px] font-bold uppercase">Silver</span>
                    </div>
                </div>
                <div class="flex flex-col items-center">
                    <i class="fa-solid fa-crown text-yellow-500 mb-1 animate-bounce"></i>
                    <img src="<%= winners[0].profile_pic || 'https://ui-avatars.com/api/?name=' + winners[0].fullname %>" class="w-16 h-16 rounded-full border-4 border-white shadow-xl mb-2">
                    <div class="podium-gold w-24 h-32 rounded-t-2xl flex flex-col items-center justify-center text-white">
                        <span class="text-4xl font-black">1</span>
                        <span class="text-[8px] font-bold uppercase">Champion</span>
                    </div>
                </div>
                <div class="flex flex-col items-center">
                    <img src="<%= winners[2].profile_pic || 'https://ui-avatars.com/api/?name=' + winners[2].fullname %>" class="w-12 h-12 rounded-full border-4 border-white shadow-lg mb-2">
                    <div class="podium-bronze w-20 h-20 rounded-t-2xl flex flex-col items-center justify-center text-white">
                        <span class="text-2xl font-black">3</span>
                        <span class="text-[8px] font-bold uppercase">Bronze</span>
                    </div>
                </div>
            </div>
        <% } %>

        <div class="space-y-3">
            <h3 class="text-[10px] font-black uppercase tracking-[0.2em] text-slate-400 ml-2 mb-4">Rankings</h3>
            
            <% winners.forEach((student, index) => { %>
                <div class="rank-card bg-white p-4 rounded-[1.5rem] flex items-center justify-between shadow-sm border border-slate-100">
                    <div class="flex items-center gap-4">
                        <span class="text-xs font-black text-slate-300 w-4">#<%= index + 1 %></span>
                        <img src="<%= student.profile_pic || 'https://ui-avatars.com/api/?name=' + student.fullname %>" class="w-10 h-10 rounded-xl object-cover">
                        <div>
                            <p class="text-sm font-black text-slate-800"><%= student.fullname %></p>
                            <p class="text-[9px] font-bold text-teal-600 uppercase"><%= student.university || 'Student' %></p>
                        </div>
                    </div>
                    <div class="text-right">
                        <p class="text-sm font-black text-slate-900"><%= student.top_score %> pts</p>
                        <p class="text-[9px] font-bold text-slate-400"><i class="fa-solid fa-stopwatch mr-1"></i><%= student.best_time %></p>
                    </div>
                </div>
            <% }) %>

            <% if (winners.length === 0) { %>
                <div class="text-center py-20">
                    <i class="fa-solid fa-trophy text-slate-200 text-5xl mb-4"></i>
                    <p class="text-slate-400 font-bold">No results yet. Be the first!</p>
                </div>
            <% } %>
        </div>

    </main>

    <nav class="fixed bottom-0 left-0 w-full bg-white/80 backdrop-blur-lg border-t border-slate-100 px-8 py-4 flex justify-between items-center z-50">
        <a href="/timeline" class="text-slate-300 hover:text-teal-600 transition-colors"><i class="fa-solid fa-house-chimney text-xl"></i></a>
        <a href="/friends" class="text-slate-300 hover:text-teal-600 transition-colors"><i class="fa-solid fa-user-group text-xl"></i></a>
        <div class="relative -mt-12">
            <a href="/quiz" class="w-16 h-16 bg-teal-800 rounded-full flex items-center justify-center text-white shadow-xl shadow-teal-900/20 border-4 border-white active:scale-90 transition-transform">
                <i class="fa-solid fa-brain text-2xl"></i>
            </a>
        </div>
        <a href="/leaderboard" class="text-teal-800"><i class="fa-solid fa-trophy text-xl"></i></a>
        <a href="/profile/<%= user.id %>" class="text-slate-300 hover:text-teal-600 transition-colors"><i class="fa-solid fa-user text-xl"></i></a>
    </nav>

</body>
</html>